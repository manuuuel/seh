# Architecture (L2)

## Overview
High-level shape (services, layers, data stores).

## Module boundaries
Each major module: responsibility + what it must NOT depend on.

## Key decisions
Link ADRs. Note anything an agent must respect (e.g. "domain layer has no I/O").
