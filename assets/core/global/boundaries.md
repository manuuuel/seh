# Boundaries — escalate, do not decide alone
- Public API / schema / migration changes; architectural changes (write an ADR).
- Deleting tests, disabling checks, or bypassing CI.
- Anything touching auth, payments, or PII.
